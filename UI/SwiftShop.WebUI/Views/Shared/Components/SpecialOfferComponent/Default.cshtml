@model List<ResultSpecialOfferDto>
@*
    //Special Offer View Component
*@

@* <div class="col-lg-4">
    <div class="product-offer mb-30" style="height: 200px;">
        <img class="img-fluid" src="~/online-shop-website-template/img/offer-1.jpg" alt="">
        <div class="offer-text">
            <h6 class="text-white text-uppercase">%20 İNDİRİM</h6>
            <h3 class="text-white mb-3">Kampanya</h3>
            <a href="" class="btn btn-primary">Alışverişe Başla</a>
        </div>
    </div>
    <div class="product-offer mb-30" style="height: 200px;">
        <img class="img-fluid" src="~/online-shop-website-template/img/offer-2.jpg" alt="">
        <div class="offer-text">
            <h6 class="text-white text-uppercase">%50 İNDİRİM</h6>
            <h3 class="text-white mb-3">Kampanya</h3>
            <a href="" class="btn btn-primary">Alışverişe Başla</a>
        </div>
    </div>
</div> *@

@* <div class="col-lg-4">
    @foreach (var item in Model)
    {
        <div class="product-offer mb-30" style="height: 200px; position: relative;">
            <img class="img-fluid w-100 h-100" src="@item.ImageUrl" style="object-fit: cover;" alt="">
            <div class="offer-text" style="position: absolute; top: 0; left: 0; padding: 20px;">
                <h6 class="text-white text-uppercase">@item.Title</h6>
                <h3 class="text-white mb-3">@item.SubTitle</h3>
                <a href="#" class="btn btn-warning text-dark">Alışverişe Başla</a>
            </div>
        </div>
    }
</div> *@

@if (Model != null && Model.Any())
{
    <div class="col-lg-4">
        @foreach (var item in Model)
        {
            <div class="product-offer mb-30" style="height: 200px;">
                <img class="img-fluid w-100 h-100" src="@item.ImageUrl" style="object-fit: cover;" alt="">
                <div class="offer-text" style="position: absolute; top: 0; left: 0; padding: 20px;">
                    <h6 class="text-white text-uppercase">@item.Title</h6>
                    <h3 class="text-white mb-3">@item.SubTitle</h3>
                    <a href="#" class="btn btn-warning text-dark">Alışverişe Başla</a>
                </div>
            </div>
        }
    </div>
}
else
{
    <p>Henüz kampanya bulunmamaktadır.</p>
}